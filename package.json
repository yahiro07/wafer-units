{
  "name": "wafer-units",
  "private": true,
  "scripts": {
    "build": "pnpm -r --if-present --filter './packages/**/*' build",
    "typecheck": "for f in $(find packages -name tsconfig.json | sort); do tsc -p \"$f\" || exit 1; done",
    "lint": "oxlint --type-aware --type-check"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "~7.0.2",
    "vite": "^8.2.2",
    "wafer-host": "^0.1.10"
  }
}
